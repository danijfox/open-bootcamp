//Notación
// Se usa el ; para delimitar el final de una linea

let num = 0;

// El . se utiliza en los objetos para acceder a los atributos

movil = {
  alto: 150,
  alto: 250,
};
console.log(movil.alto);

// los [] se utilizan para los arrays

const array = [1, 2, 3, 4, 5, 6];
array[1];
console.log(array[3]);

// los () se usan para las funciones

function hola() {
  console.log("hola");
}

hola();

// Las {} se utilizan para objetos, funciones y estructuras de control

const perro = {
  raza: "Labrador",
  pelo: "corto",
};

if (num) {
  ///
}
